<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.sirdave.sosapp.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/newContact"
            app:destination="@id/navigation_contact" />

        <action
            android:id="@+id/viewContactList"
            app:destination="@id/navigation_contact_list" />

    </fragment>

    <fragment
        android:id="@+id/navigation_contact_list"
        android:name="com.sirdave.sosapp.contact_list.ContactListFragment"
        android:label="Contact List"
        tools:layout="@layout/contact_list_fragment">

        <action
            android:id="@+id/viewContact"
            app:destination="@id/navigation_contact" />

    </fragment>

    <fragment
        android:id="@+id/navigation_contact"
        android:name="com.sirdave.sosapp.contact.ContactFragment"
        android:label="Contact"
        tools:layout="@layout/contact_fragment" />
</navigation>